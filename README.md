# **PODNO: Proper Orthogonal Decomposition Neural Operators**

We introduce **Proper Orthogonal Decomposition Neural Operators (PODNO)** for solving partial differential equations (PDEs) dominated by high-frequency components with high computational efficiency.  
This repository provides the official implementation for our paper:  
📄 [https://arxiv.org/abs/2504.18513](https://arxiv.org/abs/2504.18513)

---

## 🧩 Datasets

The datasets used in this paper are available at:

- [Download from OneDrive](https://entuedu-my.sharepoint.com/:f:/g/personal/zilan001_e_ntu_edu_sg/EmKZRJC1lVBMjicE-MHsD7IBBpNHP9-h6fZvnnoBncHVFQ?e=2R8vdo)

- **Darcy problem dataset:** adapted from [Li *et al.* (2020)](https://arxiv.org/abs/2010.08895). 
- **Synthetic sum_sin dataset:** designed as a toy example that PODNO achieves test errors two orders of magnitude lower than FNO. Since this dataset is purely synthetic and not derived from a real PDE problem, it is not included in the main paper. The generation codes are provided in this repository. 
- **Other datasets (NLS, KP):** generated by our own PDE solvers based on non-public simulation codes.

- **Pretrained model weights (.pt files):** due to large file sizes, all trained weights for **PODNO** and **FNO** are also stored in the same OneDrive link above are provided for reference only.

---

## ⚙️ Reproducing the Experiments

All experiments were conducted on a single **NVIDIA RTX A6000 GPU**.  
The random seed is fixed to **0** for reproducibility.

### **— Implementations and Results**

The implementation of **PODNO** is provided in:

```
/codes/
```

For fair comparison, the **Fourier Neural Operator (FNO)** implementation used in our experiments is also included in:

```
/FNO_baseline/
```

Some utility functions required for running the training and evaluation scripts are provided in:

```
utilities3.py
```

Please make sure to place this file in the same parent directory as the corresponding code folders when running the experiments.

---

## 🧠 PODNO Algorithm

### **Inputs**

- Training data triples $ \{ (a_j, \epsilon_j, u_j) \}_{j=1}^{M/2} $
- Number of POD layers $ L $
- Cumulative energy ratio threshold $ \rho_0 $

### **Outputs**

- Predictions $ u'_i $ for new input pairs $ (a'_i, \epsilon'_i) $

---

### **Step 1. Data Preprocessing**

Standardize the training datasets  $ \{ a_j \}_{j=1}^{M/2}, \{ \epsilon_j \}_{j=1}^{M/2}, \{ u_j \}_{j=1}^{M/2} $  using **Z-score normalization**.

---

### **Step 2. Snapshot Matrix Construction**

Form the snapshot matrix:

$$
X = [a_1, a_2, \dots, a_{M/2};\ u_1, u_2, \dots, u_{M/2}],
$$

where each column contains the standardized samples $ (a, u) $.  
Then $ X \in \mathbb{R}^{n_{\text{mesh}} \times M} $,  
with $ n_{\text{mesh}} $ denoting the number of spatial degrees of freedom.

---

### **Step 3. POD Basis Computation**

1. Compute the covariance matrix:
   $$
   C = \frac{1}{M} X X^T.
   $$
2. Perform singular value decomposition (SVD):
   $$
   C = H \Sigma^2 H^T,
   $$
   where the columns of $ H $ are orthonormal POD modes.

---

### **Step 4. Mode Truncation**

1. Compute the cumulative energy ratio:
   $$
   \rho(N) = \frac{E_N}{E_M} = \frac{\sum_{k=1}^N \lambda_k}{\sum_{k=1}^M \lambda_k}.
   $$
2. Select the smallest $ N $ such that $ \rho(N) \geq \rho_0 $.
3. Retain only the first $ N $ POD basis functions.

Define the POD transform and its inverse as:

$$
\Pi_N^{\mathrm{POD}}[v] = (\langle v, \phi_1 \rangle_{L^2}, \dots, \langle v, \phi_N \rangle_{L^2})^T,
$$
$$
(\Pi_N^{\mathrm{POD}})^{-1}[\tilde{v}] = \sum_{k=1}^N \tilde{v}_k \phi_k.
$$

---

### **Step 5. Model Training**

1. Initialize model parameters $ \theta^{(0)} \in \Theta $.
2. Load the constructed POD basis $ \Pi_N^{\mathrm{POD}} $.
3. Train the model by minimizing the loss:
   $$
   \theta^{(t+1)} = \arg\min_{\theta \in \Theta}
   \mathcal{L}(\mathcal{N}_\theta(a_j, \epsilon_j), u_j),
   $$
   obtaining the optimized parameters $ \hat{\theta} $.

---

### **Step 6. Prediction**

For new inputs $ (a'_i, \epsilon'_i) $, predict:
$
u'_i = \mathcal{N}_{\hat{\theta}}(a'_i, \epsilon'_i).
$

---

## 💬 Remarks

### **— Generation of Snapshots for Time-Dependent Equations**

There are three possible strategies for generating snapshots for time-dependent PDEs:

$$
\text{Case 1: } X_{\text{type 1}} = [u_0, u_{N_T}],
$$  
$$
\text{Case 2: } X_{\text{type 2}} = [u_0, u_1, \dots, u_{N_T}],
$$  
$$
\text{Case 3: } X_{\text{type 3}} = [u_0, u_1, \dots, u_{N_T},\ u_1 - u_0, u_2 - u_1, \dots, u_{N_T} - u_{N_T-1}].
$$

- **Efficient approach (used here):** take snapshots only from the *initial condition* and *final solution*.  
- **Higher-accuracy alternative:** if full temporal evolution is available, include intermediate time steps as additional snapshots as Case 2.

---

### **— Generation of POD Basis**

In Step 4, the POD basis can equivalently be obtained from the SVD:

$$
X = H \Sigma S^T,
$$

without explicitly computing $ X X^T $.  
Alternatively, since $ C $ is symmetric, one may compute its eigen-decomposition directly.

---

## 📘 Citation

If you find this work useful, please cite:

```bibtex
@article{cheng2025podno,
  title   = {PODNO: Proper Orthogonal Decomposition Neural Operators},
  author  = {Zilan Cheng and Zhongjian Wang and Li-Lian Wang and Mejdi Azaiez},
  journal = {arXiv preprint arXiv:2504.18513},
  year    = {2025}
}
